import React from "react";
import styles from "./ColumnRight.module.scss";
const ColumnRigt = (props) => (
  <div className={styles.columnRigt}>{props.children}</div>
);

export default ColumnRigt;
